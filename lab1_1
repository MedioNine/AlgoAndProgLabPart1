#include <stdio.h>
#include <math.h>

int main()
{
  float a=1000, b=0.0001;
  float q,w,e,RESULT;
  q = pow(a-b,3);
  w = pow(b,3);
  e = pow(a,3);
  RESULT = (q-e)/(w-3*a*a*b-3*b*b*a);
  printf ("RESULT %f\n",RESULT);
}
  
