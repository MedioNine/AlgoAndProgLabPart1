#include <stdio.h>
#include <math.h>
 int main()
{
     double a=1000.0, b=0.0001;
     double q,w,e,RESULT;
     q = pow(b,3);
     w = 3.0*a*a*b;
     e = 3.0*b*b*a;
     RESULT = (-q-w+e)/(q-w-e);
     printf ("RESULT %.27f\n",RESULT);
}
